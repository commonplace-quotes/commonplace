<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12 and later render a live preview of the real layout in the widget picker,
  which is better than a static image. previewLayout is API 31+, so it lives here rather
  than in the base file.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_description"
    android:initialLayout="@layout/widget_quote"
    android:minHeight="40dp"
    android:minWidth="180dp"
    android:previewImage="@drawable/widget_preview"
    android:previewLayout="@layout/widget_preview"
    android:resizeMode="horizontal|vertical"
    android:targetCellHeight="1"
    android:targetCellWidth="3"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
